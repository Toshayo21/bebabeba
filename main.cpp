#include <iostream>
#include <vector>
using namespace std;

enum Tile
{
	UNKNOWN,
	FILLED,
	EMPTY
};

int main()
{
	int width, height;
	vector<vector<Tile>> r;
	vector<vector<int>> hintsH;
	vector<vector<int>> hintsV;

	return 0;
}
